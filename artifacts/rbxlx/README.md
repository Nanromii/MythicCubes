# RBXLX artifacts

Đặt mọi place build dành riêng cho phase, test hoặc validation tại đây. Binary `*.rbxlx` được Git
ignore; không commit chúng. Chỉ `default-current.rbxlx` — default place canonical — được phép nằm ở
root để mở nhanh trong Roblox Studio.
